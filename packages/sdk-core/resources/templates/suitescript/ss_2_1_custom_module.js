/**
 * @NApiVersion 2.1
 */
define([${modulesDefine}],
    ${modulesJsDoc}
    (${moduleParameters}) => {

        const foo = () => {

        }

        const bar = () => {

        }

        return {foo, bar}

    });