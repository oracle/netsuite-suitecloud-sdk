# Prepare account access

Pick an existing **Auth ID** from the dropdown, or run **Manage Accounts** to create/update one.

What to verify:
- `Auth ID` dropdown has at least one valid account-role entry.
- You can refresh metadata using the refresh icon next to the dropdown.

If no account appears:
- Run **SuiteCloud: Set Up Account** first, then return to the panel.
