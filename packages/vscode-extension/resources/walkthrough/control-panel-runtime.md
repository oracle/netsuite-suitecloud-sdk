# Run local proxy

Set a valid local port, then click **Start** in the **Execution** section.

What to verify:
- **Proxy Runtime > Status** changes to `Running`.
- **Base URL** is populated and can be copied.
- Output channel shows startup logs without critical errors.

If startup fails:
- Use **Open Output** for details.
- Generate or rotate API key from the panel, then retry.
